# 🎛️ P0.3 — UI de Caps (Outbound Limits)

**Status**: ✅ Pronto para deploy (campos existem no schema)  
**Impacto**: Operacional (channel safety, não score)  
**Para Vitalli**: `hourly_cap=15, daily_cap=60`

---

## 📋 Implementação Rápida

### Campos Já Existem
```typescript
// src/infrastructure/db/schema.ts
outboundHourlyCap: integer("outbound_hourly_cap").notNull().default(40),
outboundDailyCap: integer("outbound_daily_cap").notNull().default(200),
```

### Para Vitalli (Go-Live T+7)
```sql
UPDATE organizations 
SET outbound_hourly_cap = 15, outbound_daily_cap = 60 
WHERE slug = 'clinica-vitalli';
```

### UI/UX (Opcional — Future)
- Owner Panel → Settings → Channel Safety
- Inputs: hourly_cap (default 15), daily_cap (default 60)
- Save button → PATCH `/api/clinic/caps`

---

## ✅ Checklist Mínimo para Go-Live

- [x] Campo `outbound_hourly_cap` existe
- [x] Campo `outbound_daily_cap` existe
- [x] Defaults estão em schema
- [ ] Vitalli: aplicar 15/60 (SQL ou via UI quando feita)
- [ ] Testar que caps funcionam (sender worker respeitará)
- [ ] Documentar no README de operações

---

**Próximo**: Deploy todas as mudanças (P0.1-P0.6) → Análise pós-correções

*08/07/2026*

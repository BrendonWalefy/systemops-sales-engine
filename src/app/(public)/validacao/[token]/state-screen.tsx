/**
 * Re-export de compatibilidade: `StateScreen` foi promovido para
 * `(public)/state-screen.tsx` (feature catalogada em docs/features.md,
 * Apêndice B) para ser compartilhado com `/conversas/[token]`. Mantido aqui
 * para não alterar o import em `page.tsx`.
 */
export { StateScreen } from "../../state-screen";

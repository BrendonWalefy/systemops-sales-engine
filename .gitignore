node_modules
.next
out
dist
coverage
.env*
!.env.example
*.log
*.tsbuildinfo
.DS_Store
.vercel
.claude/
tmp/

# Mídia local da demo (sobe pro Blob via scripts/upload-demo-media.ts)
scripts/demo-media/

# Transcripts de teste / exports de conversa com PII de leads — nunca versionar
docs/testing/
# Mídia local de clínicas reais (sobe pro Blob via scripts/upload-nc-media.ts)
scripts/nc-media/

# Exports de calendário de clientes (nomes de pacientes + valores cobrados) —
# nunca versionar, usados só localmente para testar o parser de import
public/*.ical.zip
public/*.ics
*.ics

node_modules
.next
out
dist
.site-build/
coverage
.env*
!.env.example
*.log
*.tsbuildinfo
.DS_Store
.vercel
.claude/
tmp/

# Mídia local da demo (sobe pro Blob via scripts/upload-demo-media.ts)
scripts/demo-media/

# Transcripts de teste / exports de conversa com PII de leads — nunca versionar
docs/testing/
# Mídia local de clínicas reais (sobe pro Blob via scripts/upload-nc-media.ts)
scripts/nc-media/

# Exports de calendário de clientes (nomes de pacientes + valores cobrados) —
# nunca versionar, usados só localmente para testar o parser de import
public/*.ical.zip
public/*.ics
*.ics

# Artefatos de scratch do harness de replay/regressão (mensagens reais de leads
# = PII) — gerados por scripts/extract-replay-cases.ts e afins. Nunca versionar.
*_cases.json
*_messages.json
*_replay_results*.txt
mensagens_operador_*.txt
*.needs-review.json
replay-datasets/
vitalli-last-*.json

# Scripts de investigação descartáveis (data-munging pontual, IDs hardcoded).
# Ficam locais; não entram no verify nem no repo.
scripts/scratch/
